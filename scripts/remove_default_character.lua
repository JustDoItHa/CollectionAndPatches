--modinfo 配置
--AddOptionHeader("自定义禁用角色"),
--AddOption("remove_default_character_switch", "自定义禁用角色开关", "是否开启自定义禁用角色功能", false),
--{
---- 配置项名换，在modmain.lua里获取配置值时要用到
--name = "allclosure",
----代替name的显示
--label = "全部删除!",
---- 鼠标移到配置项上时所显示的信息
---- 可选项选中时的值，在modmain.lua里获取到的值就是这个数据，类型可以为整形，布尔，浮点，字符串
--hover = "你一定是玩人物Mod的是吧？",
---- 配置项目可选项
--options = {
--{
---- 可选项上显示的内容
--description = "False",
--data = false,
---- 鼠标移动到可选项上显示的信息
---- 可选项选中时的值，在modmain.lua里获取到的值就是这个数据，类型可以为整形，布尔，浮点，字符串
--hover = "请把下面的选项全部关掉！",
--},
--{
---- 可选项上显示的内容
--description = "True",
--data = true,
---- 鼠标移动到可选项上显示的信息
---- 可选项选中时的值，在modmain.lua里获取到的值就是这个数据，类型可以为整形，布尔，浮点，字符串
--hover = "请把下面的选项全部关掉！",
--}, },
---- 默认值，与可选项里的值匹配作为默认值
----默认关闭
--default = true
--},
--{
---- 配置项名换，在modmain.lua里获取配置值时要用到
--name = "wilsonclosure",
----代替name的显示
--label = "是否删除威尔逊？",
---- 鼠标移到配置项上时所显示的信息
---- 可选项选中时的值，在modmain.lua里获取到的值就是这个数据，类型可以为整形，布尔，浮点，字符串
--hover = "？？？",
---- 配置项目可选项
--options = {
--{
---- 可选项上显示的内容
--description = "False",
--data = false,
---- 鼠标移动到可选项上显示的信息
---- 可选项选中时的值，在modmain.lua里获取到的值就是这个数据，类型可以为整形，布尔，浮点，字符串
--hover = "不删除威尔逊",
--},
--{
---- 可选项上显示的内容
--description = "True",
--data = true,
---- 鼠标移动到可选项上显示的信息
---- 可选项选中时的值，在modmain.lua里获取到的值就是这个数据，类型可以为整形，布尔，浮点，字符串
--hover = "删除威尔逊",
--}, },
---- 默认值，与可选项里的值匹配作为默认值
----默认关闭
--default = false
--},
--{
---- 配置项名换，在modmain.lua里获取配置值时要用到
--name = "willowclosure",
----代替name的显示
--label = "是否删除薇洛？",
---- 鼠标移到配置项上时所显示的信息
---- 可选项选中时的值，在modmain.lua里获取到的值就是这个数据，类型可以为整形，布尔，浮点，字符串
--hover = "？？？",
---- 配置项目可选项
--options = {
--{
---- 可选项上显示的内容
--description = "False",
--data = false,
---- 鼠标移动到可选项上显示的信息
---- 可选项选中时的值，在modmain.lua里获取到的值就是这个数据，类型可以为整形，布尔，浮点，字符串
--hover = "不删除薇洛",
--},
--{
---- 可选项上显示的内容
--description = "True",
--data = true,
---- 鼠标移动到可选项上显示的信息
---- 可选项选中时的值，在modmain.lua里获取到的值就是这个数据，类型可以为整形，布尔，浮点，字符串
--hover = "删除薇洛",
--}, },
---- 默认值，与可选项里的值匹配作为默认值
----默认关闭
--default = false
--},
--{
---- 配置项名换，在modmain.lua里获取配置值时要用到
--name = "wolfgangclosure",
----代替name的显示
--label = "是否删除沃尔夫冈？",
---- 鼠标移到配置项上时所显示的信息
---- 可选项选中时的值，在modmain.lua里获取到的值就是这个数据，类型可以为整形，布尔，浮点，字符串
--hover = "？？？",
---- 配置项目可选项
--options = {
--{
---- 可选项上显示的内容
--description = "False",
--data = false,
---- 鼠标移动到可选项上显示的信息
---- 可选项选中时的值，在modmain.lua里获取到的值就是这个数据，类型可以为整形，布尔，浮点，字符串
--hover = "不删除沃尔夫冈",
--},
--{
---- 可选项上显示的内容
--description = "True",
--data = true,
---- 鼠标移动到可选项上显示的信息
---- 可选项选中时的值，在modmain.lua里获取到的值就是这个数据，类型可以为整形，布尔，浮点，字符串
--hover = "删除沃尔夫冈",
--}, },
---- 默认值，与可选项里的值匹配作为默认值
----默认关闭
--default = false
--},
--{
---- 配置项名换，在modmain.lua里获取配置值时要用到
--name = "wendyclosure",
----代替name的显示
--label = "是否删除温蒂？",
---- 鼠标移到配置项上时所显示的信息
---- 可选项选中时的值，在modmain.lua里获取到的值就是这个数据，类型可以为整形，布尔，浮点，字符串
--hover = "？？？",
---- 配置项目可选项
--options = {
--{
---- 可选项上显示的内容
--description = "False",
--data = false,
---- 鼠标移动到可选项上显示的信息
---- 可选项选中时的值，在modmain.lua里获取到的值就是这个数据，类型可以为整形，布尔，浮点，字符串
--hover = "不删除温蒂",
--},
--{
---- 可选项上显示的内容
--description = "True",
--data = true,
---- 鼠标移动到可选项上显示的信息
---- 可选项选中时的值，在modmain.lua里获取到的值就是这个数据，类型可以为整形，布尔，浮点，字符串
--hover = "删除温蒂",
--}, },
---- 默认值，与可选项里的值匹配作为默认值
----默认关闭
--default = false
--},
--{
---- 配置项名换，在modmain.lua里获取配置值时要用到
--name = "wx78closure",
----代替name的显示
--label = "是否删除wx78？",
---- 鼠标移到配置项上时所显示的信息
---- 可选项选中时的值，在modmain.lua里获取到的值就是这个数据，类型可以为整形，布尔，浮点，字符串
--hover = "？？？",
---- 配置项目可选项
--options = {
--{
---- 可选项上显示的内容
--description = "False",
--data = false,
---- 鼠标移动到可选项上显示的信息
---- 可选项选中时的值，在modmain.lua里获取到的值就是这个数据，类型可以为整形，布尔，浮点，字符串
--hover = "不删除wx78",
--},
--{
---- 可选项上显示的内容
--description = "True",
--data = true,
---- 鼠标移动到可选项上显示的信息
---- 可选项选中时的值，在modmain.lua里获取到的值就是这个数据，类型可以为整形，布尔，浮点，字符串
--hover = "删除wx78",
--}, },
---- 默认值，与可选项里的值匹配作为默认值
----默认关闭
--default = false
--},
--{
---- 配置项名换，在modmain.lua里获取配置值时要用到
--name = "wickerbottomclosure",
----代替name的显示
--label = "是否删除薇克巴顿？",
---- 鼠标移到配置项上时所显示的信息
---- 可选项选中时的值，在modmain.lua里获取到的值就是这个数据，类型可以为整形，布尔，浮点，字符串
--hover = "？？？",
---- 配置项目可选项
--options = {
--{
---- 可选项上显示的内容
--description = "False",
--data = false,
---- 鼠标移动到可选项上显示的信息
---- 可选项选中时的值，在modmain.lua里获取到的值就是这个数据，类型可以为整形，布尔，浮点，字符串
--hover = "不删除薇克巴顿",
--},
--{
---- 可选项上显示的内容
--description = "True",
--data = true,
---- 鼠标移动到可选项上显示的信息
---- 可选项选中时的值，在modmain.lua里获取到的值就是这个数据，类型可以为整形，布尔，浮点，字符串
--hover = "删除薇克巴顿",
--}, },
---- 默认值，与可选项里的值匹配作为默认值
----默认关闭
--default = false
--},
--{
---- 配置项名换，在modmain.lua里获取配置值时要用到
--name = "woodieclosure",
----代替name的显示
--label = "是否删除伍迪？",
---- 鼠标移到配置项上时所显示的信息
---- 可选项选中时的值，在modmain.lua里获取到的值就是这个数据，类型可以为整形，布尔，浮点，字符串
--hover = "？？？",
---- 配置项目可选项
--options = {
--{
---- 可选项上显示的内容
--description = "False",
--data = false,
---- 鼠标移动到可选项上显示的信息
---- 可选项选中时的值，在modmain.lua里获取到的值就是这个数据，类型可以为整形，布尔，浮点，字符串
--hover = "不删除伍迪",
--},
--{
---- 可选项上显示的内容
--description = "True",
--data = true,
---- 鼠标移动到可选项上显示的信息
---- 可选项选中时的值，在modmain.lua里获取到的值就是这个数据，类型可以为整形，布尔，浮点，字符串
--hover = "删除伍迪",
--}, },
---- 默认值，与可选项里的值匹配作为默认值
----默认关闭
--default = false
--},
--{
---- 配置项名换，在modmain.lua里获取配置值时要用到
--name = "wesclosure",
----代替name的显示
--label = "是否删除韦斯？",
---- 鼠标移到配置项上时所显示的信息
---- 可选项选中时的值，在modmain.lua里获取到的值就是这个数据，类型可以为整形，布尔，浮点，字符串
--hover = "？？？",
---- 配置项目可选项
--options = {
--{
---- 可选项上显示的内容
--description = "False",
--data = false,
---- 鼠标移动到可选项上显示的信息
---- 可选项选中时的值，在modmain.lua里获取到的值就是这个数据，类型可以为整形，布尔，浮点，字符串
--hover = "不删除韦斯",
--},
--{
---- 可选项上显示的内容
--description = "True",
--data = true,
---- 鼠标移动到可选项上显示的信息
---- 可选项选中时的值，在modmain.lua里获取到的值就是这个数据，类型可以为整形，布尔，浮点，字符串
--hover = "(最强的你都要删?)",
--}, },
---- 默认值，与可选项里的值匹配作为默认值
----默认关闭
--default = false
--},
--{
---- 配置项名换，在modmain.lua里获取配置值时要用到
--name = "waxwellclosure",
----代替name的显示
--label = "是否删除麦斯威尔？",
---- 鼠标移到配置项上时所显示的信息
---- 可选项选中时的值，在modmain.lua里获取到的值就是这个数据，类型可以为整形，布尔，浮点，字符串
--hover = "？？？",
---- 配置项目可选项
--options = {
--{
---- 可选项上显示的内容
--description = "False",
--data = false,
---- 鼠标移动到可选项上显示的信息
---- 可选项选中时的值，在modmain.lua里获取到的值就是这个数据，类型可以为整形，布尔，浮点，字符串
--hover = "不删除麦斯威尔",
--},
--{
---- 可选项上显示的内容
--description = "True",
--data = true,
---- 鼠标移动到可选项上显示的信息
---- 可选项选中时的值，在modmain.lua里获取到的值就是这个数据，类型可以为整形，布尔，浮点，字符串
--hover = "删除麦斯威尔",
--}, },
---- 默认值，与可选项里的值匹配作为默认值
----默认关闭
--default = false
--},
--{
---- 配置项名换，在modmain.lua里获取配置值时要用到
--name = "wathgrithrclosure",
----代替name的显示
--label = "是否删除薇格弗德(女武神)？",
---- 鼠标移到配置项上时所显示的信息
---- 可选项选中时的值，在modmain.lua里获取到的值就是这个数据，类型可以为整形，布尔，浮点，字符串
--hover = "？？？",
---- 配置项目可选项
--options = {
--{
---- 可选项上显示的内容
--description = "False",
--data = false,
---- 鼠标移动到可选项上显示的信息
---- 可选项选中时的值，在modmain.lua里获取到的值就是这个数据，类型可以为整形，布尔，浮点，字符串
--hover = "不删除薇格弗德",
--},
--{
---- 可选项上显示的内容
--description = "True",
--data = true,
---- 鼠标移动到可选项上显示的信息
---- 可选项选中时的值，在modmain.lua里获取到的值就是这个数据，类型可以为整形，布尔，浮点，字符串
--hover = "删除薇格弗德",
--}, },
---- 默认值，与可选项里的值匹配作为默认值
----默认关闭
--default = false
--},
--{
---- 配置项名换，在modmain.lua里获取配置值时要用到
--name = "webberclosure",
----代替name的显示
--label = "是否删除韦伯？",
---- 鼠标移到配置项上时所显示的信息
---- 可选项选中时的值，在modmain.lua里获取到的值就是这个数据，类型可以为整形，布尔，浮点，字符串
--hover = "？？？",
---- 配置项目可选项
--options = {
--{
---- 可选项上显示的内容
--description = "False",
--data = false,
---- 鼠标移动到可选项上显示的信息
---- 可选项选中时的值，在modmain.lua里获取到的值就是这个数据，类型可以为整形，布尔，浮点，字符串
--hover = "不删除韦伯",
--},
--{
---- 可选项上显示的内容
--description = "True",
--data = true,
---- 鼠标移动到可选项上显示的信息
---- 可选项选中时的值，在modmain.lua里获取到的值就是这个数据，类型可以为整形，布尔，浮点，字符串
--hover = "删除韦伯",
--}, },
---- 默认值，与可选项里的值匹配作为默认值
----默认关闭
--default = false
--},
--{
---- 配置项名换，在modmain.lua里获取配置值时要用到
--name = "winonaclosure",
----代替name的显示
--label = "是否删除薇诺娜？",
---- 鼠标移到配置项上时所显示的信息
---- 可选项选中时的值，在modmain.lua里获取到的值就是这个数据，类型可以为整形，布尔，浮点，字符串
--hover = "？？？",
---- 配置项目可选项
--options = {
--{
---- 可选项上显示的内容
--description = "False",
--data = false,
---- 鼠标移动到可选项上显示的信息
---- 可选项选中时的值，在modmain.lua里获取到的值就是这个数据，类型可以为整形，布尔，浮点，字符串
--hover = "不删除薇诺娜",
--},
--{
---- 可选项上显示的内容
--description = "True",
--data = true,
---- 鼠标移动到可选项上显示的信息
---- 可选项选中时的值，在modmain.lua里获取到的值就是这个数据，类型可以为整形，布尔，浮点，字符串
--hover = "删除薇诺娜",
--}, },
---- 默认值，与可选项里的值匹配作为默认值
----默认关闭
--default = false
--},
--{
---- 配置项名换，在modmain.lua里获取配置值时要用到
--name = "warlyclosure",
----代替name的显示
--label = "是否删除沃利？",
---- 鼠标移到配置项上时所显示的信息
---- 可选项选中时的值，在modmain.lua里获取到的值就是这个数据，类型可以为整形，布尔，浮点，字符串
--hover = "？？？",
---- 配置项目可选项
--options = {
--{
---- 可选项上显示的内容
--description = "False",
--data = false,
---- 鼠标移动到可选项上显示的信息
---- 可选项选中时的值，在modmain.lua里获取到的值就是这个数据，类型可以为整形，布尔，浮点，字符串
--hover = "不删除沃利",
--},
--{
---- 可选项上显示的内容
--description = "True",
--data = true,
---- 鼠标移动到可选项上显示的信息
---- 可选项选中时的值，在modmain.lua里获取到的值就是这个数据，类型可以为整形，布尔，浮点，字符串
--hover = "删除沃利",
--}, },
---- 默认值，与可选项里的值匹配作为默认值
----默认关闭
--default = false
--},
--{
---- 配置项名换，在modmain.lua里获取配置值时要用到
--name = "wortoxclosure",
----代替name的显示
--label = "是否删除沃拓克斯？",
---- 鼠标移到配置项上时所显示的信息
---- 可选项选中时的值，在modmain.lua里获取到的值就是这个数据，类型可以为整形，布尔，浮点，字符串
--hover = "？？？",
---- 配置项目可选项
--options = {
--{
---- 可选项上显示的内容
--description = "False",
--data = false,
---- 鼠标移动到可选项上显示的信息
---- 可选项选中时的值，在modmain.lua里获取到的值就是这个数据，类型可以为整形，布尔，浮点，字符串
--hover = "不删除沃拓克斯",
--},
--{
---- 可选项上显示的内容
--description = "True",
--data = true,
---- 鼠标移动到可选项上显示的信息
---- 可选项选中时的值，在modmain.lua里获取到的值就是这个数据，类型可以为整形，布尔，浮点，字符串
--hover = "删除沃拓克斯",
--}, },
---- 默认值，与可选项里的值匹配作为默认值
----默认关闭
--default = false
--},
--{
---- 配置项名换，在modmain.lua里获取配置值时要用到
--name = "wormwoodclosure",
----代替name的显示
--label = "是否删除沃姆伍德？",
---- 鼠标移到配置项上时所显示的信息
---- 可选项选中时的值，在modmain.lua里获取到的值就是这个数据，类型可以为整形，布尔，浮点，字符串
--hover = "？？？",
---- 配置项目可选项
--options = {
--{
---- 可选项上显示的内容
--description = "False",
--data = false,
---- 鼠标移动到可选项上显示的信息
---- 可选项选中时的值，在modmain.lua里获取到的值就是这个数据，类型可以为整形，布尔，浮点，字符串
--hover = "不删除沃姆伍德",
--},
--{
---- 可选项上显示的内容
--description = "True",
--data = true,
---- 鼠标移动到可选项上显示的信息
---- 可选项选中时的值，在modmain.lua里获取到的值就是这个数据，类型可以为整形，布尔，浮点，字符串
--hover = "删除沃姆伍德",
--}, },
---- 默认值，与可选项里的值匹配作为默认值
----默认关闭
--default = false
--},
--{
---- 配置项名换，在modmain.lua里获取配置值时要用到
--name = "wurtclosure",
----代替name的显示
--label = "是否删除沃特？",
---- 鼠标移到配置项上时所显示的信息
---- 可选项选中时的值，在modmain.lua里获取到的值就是这个数据，类型可以为整形，布尔，浮点，字符串
--hover = "？？？",
---- 配置项目可选项
--options = {
--{
---- 可选项上显示的内容
--description = "False",
--data = false,
---- 鼠标移动到可选项上显示的信息
---- 可选项选中时的值，在modmain.lua里获取到的值就是这个数据，类型可以为整形，布尔，浮点，字符串
--hover = "不删除沃特",
--},
--{
---- 可选项上显示的内容
--description = "True",
--data = true,
---- 鼠标移动到可选项上显示的信息
---- 可选项选中时的值，在modmain.lua里获取到的值就是这个数据，类型可以为整形，布尔，浮点，字符串
--hover = "删除沃特",
--}, },
---- 默认值，与可选项里的值匹配作为默认值
----默认关闭
--default = false
--},
--{
---- 配置项名换，在modmain.lua里获取配置值时要用到
--name = "walterclosure",
----代替name的显示
--label = "是否删除沃尔特？",
---- 鼠标移到配置项上时所显示的信息
---- 可选项选中时的值，在modmain.lua里获取到的值就是这个数据，类型可以为整形，布尔，浮点，字符串
--hover = "？？？",
---- 配置项目可选项
--options = {
--{
---- 可选项上显示的内容
--description = "False",
--data = false,
---- 鼠标移动到可选项上显示的信息
---- 可选项选中时的值，在modmain.lua里获取到的值就是这个数据，类型可以为整形，布尔，浮点，字符串
--hover = "不删除沃尔特",
--},
--{
---- 可选项上显示的内容
--description = "True",
--data = true,
---- 鼠标移动到可选项上显示的信息
---- 可选项选中时的值，在modmain.lua里获取到的值就是这个数据，类型可以为整形，布尔，浮点，字符串
--hover = "删除沃尔特",
--}, },
---- 默认值，与可选项里的值匹配作为默认值
----默认关闭
--default = false
--},
--{
---- 配置项名换，在modmain.lua里获取配置值时要用到
--name = "wandaclosure",
----代替name的显示
--label = "是否删除旺达？",
---- 鼠标移到配置项上时所显示的信息
---- 可选项选中时的值，在modmain.lua里获取到的值就是这个数据，类型可以为整形，布尔，浮点，字符串
--hover = "？？？",
---- 配置项目可选项
--options = {
--{
---- 可选项上显示的内容
--description = "False",
--data = false,
---- 鼠标移动到可选项上显示的信息
---- 可选项选中时的值，在modmain.lua里获取到的值就是这个数据，类型可以为整形，布尔，浮点，字符串
--hover = "不删除旺达",
--},
--{
---- 可选项上显示的内容
--description = "True",
--data = true,
---- 鼠标移动到可选项上显示的信息
---- 可选项选中时的值，在modmain.lua里获取到的值就是这个数据，类型可以为整形，布尔，浮点，字符串
--hover = "删除旺达",
--}, },
---- 默认值，与可选项里的值匹配作为默认值
----默认关闭
--default = false
--},


--获得全局变量(习惯了)
GLOBAL.setmetatable(
        env,
        {
            __index = function(t, k)
                return GLOBAL.rawget(GLOBAL, k)
            end
        }
)
--:D
local allclosure = GetModConfigData("allclosure")
if allclosure == true then
    RemoveDefaultCharacter("wilson")
    RemoveDefaultCharacter("willow")
    RemoveDefaultCharacter("wolfgang")
    RemoveDefaultCharacter("wendy")
    RemoveDefaultCharacter("wx78")
    RemoveDefaultCharacter("wickerbottom")
    RemoveDefaultCharacter("woodie")
    RemoveDefaultCharacter("wes")
    RemoveDefaultCharacter("waxwell")
    RemoveDefaultCharacter("wathgrithr")
    RemoveDefaultCharacter("webber")
    RemoveDefaultCharacter("winona")
    RemoveDefaultCharacter("warly")
    RemoveDefaultCharacter("wortox")
    RemoveDefaultCharacter("wormwood")
    RemoveDefaultCharacter("wurt")
    RemoveDefaultCharacter("walter")
    RemoveDefaultCharacter("wanda")
end

local wilsonclosure = GetModConfigData("wilsonclosure")
if wilsonclosure == true then
    RemoveDefaultCharacter("wilson")
end

local willowclosure = GetModConfigData("willowclosure")
if willowclosure == true then
    RemoveDefaultCharacter("willow")
end

local wolfgangclosure = GetModConfigData("wolfgangclosure")
if wolfgangclosure == true then
    RemoveDefaultCharacter("wolfgang")
end

local wendyclosure = GetModConfigData("wendyclosure")
if wendyclosure == true then
    RemoveDefaultCharacter("wendy")
end

local wx78closure = GetModConfigData("wx78closure")
if wx78closure == true then
    RemoveDefaultCharacter("wx78")
end

local wickerbottomclosure = GetModConfigData("wickerbottomclosure")
if wickerbottomclosure == true then
    RemoveDefaultCharacter("wickerbottom")
end

local woodieclosure = GetModConfigData("woodieclosure")
if woodieclosure == true then
    RemoveDefaultCharacter("woodie")
end

local wesclosure = GetModConfigData("wesclosure")
if wesclosure == true then
    RemoveDefaultCharacter("wes")
end

local waxwellclosure = GetModConfigData("waxwellclosure")
if waxwellclosure == true then
    RemoveDefaultCharacter("waxwell")
end

local wathgrithrclosure = GetModConfigData("wathgrithrclosure")
if wathgrithrclosure == true then
    RemoveDefaultCharacter("wathgrithr")
end

local webberclosure = GetModConfigData("webberclosure")
if webberclosure == true then
    RemoveDefaultCharacter("webber")
end

local winonaclosure = GetModConfigData("winonaclosure")
if winonaclosure == true then
    RemoveDefaultCharacter("winona")
end

local warlyclosure = GetModConfigData("warlyclosure")
if warlyclosure == true then
    RemoveDefaultCharacter("warly")
end

local wortoxclosure = GetModConfigData("wortoxclosure")
if wortoxclosure == true then
    RemoveDefaultCharacter("wortox")
end

local wormwoodclosure = GetModConfigData("wormwoodclosure")
if wormwoodclosure == true then
    RemoveDefaultCharacter("wormwood")
end

local wurtclosure = GetModConfigData("wurtclosure")
if wurtclosure == true then
    RemoveDefaultCharacter("wurt")
end

local walterclosure = GetModConfigData("walterclosure")
if walterclosure == true then
    RemoveDefaultCharacter("walter")
end

local wandaclosure = GetModConfigData("wandaclosure")
if wandaclosure == true then
    RemoveDefaultCharacter("wanda")
end